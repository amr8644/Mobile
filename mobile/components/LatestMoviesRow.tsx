import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LatestMoviesRow = () => {
   return (
      <View>
         <Text>LatestMoviesRow</Text>
      </View>
   );
};

export default LatestMoviesRow;

const styles = StyleSheet.create({});
